version: 2
jobs:
  build:
    steps:
      - run: echo not running
      
workflows:
  version: 2
  dont-build:
    jobs:
      - fake-build:
          filters:
            branches:
              only: wrong-name-so-dont-deploy #hack to stop gh-pages branch building
